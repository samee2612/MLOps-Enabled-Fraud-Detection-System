# This is your main application code
def greet(name):
    return f"Hello, {name}!"

def add_numbers(a, b):
    return a + b

# Example of a linter error (too many blank lines, or trailing whitespace)
